<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.regex.mapper.QuizMapper">
    <!-- <select id="selectAll" resultMap="quizMap">
        select
            q.quiz,
            c.answer1,
            c.answer2,
            c.answer3
        from
            quiz q
        JOIN
            choice c
        ON
            q.id = c.quiz_id
        WHERE
            q.is_deleted = false
        AND
            c.is_deleted = false
    </select>

    <resultMap id="quizMap" type="com.regex.entity.Quiz">
        <id property="id" column="id" />
        <result property="quiz" column="quiz" />
        <collection property="choices" ofType="com.regex.entity.Choice">
            <result property="answer1" column="answer1" />
            <result property="answer2" column="answer2" />
            <result property="answer3" column="answer3" />
        </collection>
    </resultMap> -->

    <select id="selectAll" resultType="com.regex.entity.Quiz">
        select id, quiz, answer1, answer2, answer3 from quiz
    </select>

    <!-- <select id="selectAllChoice" resultType="com.regex.entity.Choice">
        select quiz_id, answer1, answer2, answer3 from choice
    </select> -->
</mapper>